long secs = $0 / 1000;
long mins = secs / 60;
secs %= 60;
long hours = mins / 60;
mins %= 60;
long days = hours / 24;
hours %= 24;
long weeks = days / 7;
days %= 7;
long months = weeks / 4;
weeks %= 4;
long years = months / 12;
months %= 12;
echo(years + " years, " + months + " months, " + weeks + " weeks, " + days + " days, " + hours + ":" + mins + ":" + secs);